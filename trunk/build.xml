<project name="personas" basedir=".">
    <description>
        build personas for songbird
    </description>

    <!-- chrome jar -->
    <delete  file="chrome/personas.jar"/>
    <jar destfile="chrome/personas.jar" basedir="chrome" />
		

    <!-- Zip the results into an xpi file -->
    <delete  file="personas-sb-0.1.xpi"/>
    <zip destfile="personas-sb-0.1.xpi" basedir="./" excludes="build.xml, run.bat"/>

</project>
