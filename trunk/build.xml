<project name="personas" basedir=".">
    <description>
        build personas for songbird
    </description>

    <!-- Zip the results into an xpi file -->
    <delete  file="personas-sb-0.8.2.xpi"/>
    <zip destfile="personas-sb-0.8.2.xpi" basedir="./" excludes="build.xml, run.bat"/>

</project>
